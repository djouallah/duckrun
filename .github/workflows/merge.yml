name: merge-spill

# Rigorous incremental-MERGE smoke test on a big fact table: generate a large TPCH lineitem
# (default SF=20, ~120M rows) as the target Delta table, then merge a small (~0.1%) sample with
# randomized measures back into it THREE times (successive incremental loads), covering both
# UPDATE (matched) and INSERT (key-shifted) branches. No artificial memory cgroup — the question
# is whether duckrun completes on the runner's real 16 GB RAM, with its shipping defaults on
# (cgroup-aware DuckDB memory_limit + delta_rs merge spill). Runs against the pinned
# deltalake 1.5.0 (1.6.0's merge is broken at scale).
#
# Expensive (~10 min), so it does NOT run on every push. It gates a release: the publish
# workflow calls it (workflow_call) on a version tag and only publishes if it passes. Can
# also be run by hand via workflow_dispatch.

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      sf:
        description: TPCH scale factor for the target fact table
        default: "20"
      sample_frac:
        description: fraction of the fact sampled per incremental round
        default: "0.001"
      rounds:
        description: number of sequential incremental merges
        default: "3"

jobs:
  merge-spill:
    runs-on: ubuntu-latest
    env:
      SF: ${{ github.event.inputs.sf || '20' }}
      SAMPLE_FRAC: ${{ github.event.inputs.sample_frac || '0.001' }}
      ROUNDS: ${{ github.event.inputs.rounds || '3' }}
      DIR: /mnt/mbench   # the big (~66 GB) runner disk; / has only ~14 GB free
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install duckrun + tpchgen-cli
        run: |
          pip install -e ".[test]"
          pip install tpchgen-cli

      - name: Show RAM / disk / deltalake version (must be 1.5.0)
        run: |
          free -h
          df -h /mnt /
          DLV=$(python -c "import deltalake; print(deltalake.__version__)")
          echo "deltalake $DLV"
          python -c "import deltalake,sys; sys.exit(0 if deltalake.__version__.startswith('1.5.') else 1)"
          echo "DLV=$DLV" >> "$GITHUB_ENV"

      - name: Prepare big disk dir
        run: sudo mkdir -p "$DIR" && sudo chown "$(id -u):$(id -g)" "$DIR"

      - name: Generate fact + run incremental merges
        # The bench writes the detailed, data-engineer-readable report straight to
        # $GITHUB_STEP_SUMMARY on completion (pass or assertion-fail). We only add a fallback
        # line here for the case where it dies before writing — i.e. OOM-killed (rc 137/139).
        run: |
          set -o pipefail
          python tools/merge_tpch_bench.py --dir "$DIR" \
            --sf "$SF" --sample-frac "$SAMPLE_FRAC" --rounds "$ROUNDS" ; rc=$?
          if [ $rc -eq 137 ] || [ $rc -eq 139 ]; then
            {
              echo "## 🔀 Incremental MERGE test — duckrun on Delta Lake"
              echo
              echo "Merging a ~${SAMPLE_FRAC} sample into a SF=${SF} lineitem table ${ROUNDS}× on the runner's real RAM."
              echo
              echo "**Result: 💀 OOM-killed (rc=$rc)** — duckrun's defaults did not keep the merge under the runner's RAM."
            } >> "$GITHUB_STEP_SUMMARY"
          fi
          exit $rc
